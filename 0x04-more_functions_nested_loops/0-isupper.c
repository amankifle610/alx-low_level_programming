#include "main.h"
/**
 * _isupper - checks for uppercase character.
 * @c : alpha
 * Return: 0
*/
int _isupper(int c)
{
	char i;

	i = 'a';
	while (i <= 'z')
	{
		if (a == i)
		return (1);
		i++;
	}
	return (0);
}
